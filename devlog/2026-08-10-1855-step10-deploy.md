# 进度日志 10 · 部署 v3 到日常启动目录（2026-08-10 下午 6:55）

- **时间**：2026-08-10 18:55
- **会话**：ailauncher-v3-rewrite（后台 job abc440da）续
- **性质**：用户交互请求——「整理一份放到 D:\Coding\Scripts\ailauncher，把现在的 node 脚本换掉」。

## 路径纠正（重要）

用户给的 `D:\Coding\Scripts\ailauncher` **不存在**。真实部署目标是 **`D:\Coding\Scripts\ai-launcher`**（带连字符），且该目录已在 PATH（`where` 确认）。若新建 `ailauncher` 空目录反而不会被 PATH 命中，所以部署到 `ai-launcher`。

## 做了什么

1. 拷贝新构建的 `ailauncher.exe` → `D:\Coding\Scripts\ai-launcher\`。
2. **就地 `--migrate`**：`config.json` 从旧 `tools[]` 迁移为 v3 `agents[]`（5 agent 全保留，含 codex 的绝对 exec 路径）；`data.json` 归一化。二次运行 no-op 幂等。
3. 归档旧 Node 脚本：`ailauncher.js` → `.js.bak`、`ailauncher.cmd` → `.cmd.bak`（可回滚）。
4. 拷贝 `README.md` 作使用参考。
5. 补 `config.json.bak-v3`（迁移前 tools[] 原版）。

## 验证

- 从非部署目录（用户主目录）裸命令 `ailauncher --version` → 0.3.0-dev；`--list` → 5 agent 齐全。命令解析到 `D:\Coding\Scripts\ai-launcher\ailauncher.exe`。
- 迁移内容零丢失：与 dev `.bak-v3` diff 仅差 omitempty 空字段与换行（格式层）。
- 用户日常 `data.json`（opencode/claude/codex 的目录历史/removedDirs/rootDir/seeded）**原样保留**，v3 直接继承。

## 注意 / 下一步

- 旧 Node 脚本已停用但未删（`.bak` 在，反手可还原）；确认 v3 好用后可删 `.bak` 与 `config.json.bak-v3`。
- 真机验收不变：`ailauncher`（TUI）、`ailauncher -i claude [path]`（交互直达）、改 `config.json` 的 `terminal` 试 wt/wezterm。
- 开发源目录 `AI_LAUNCHER` 仍是唯一事实来源；日常目录是部署副本。后续改动重 `go build` 后把新 exe 拷过去即可。
